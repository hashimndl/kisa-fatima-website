# Security Policy

This project includes baseline security headers, CSP, HSTS, X-Frame-Options, Referrer Policy, Permissions Policy, API honeypot checks, and simple in-memory API rate limiting.

Do not commit secrets. Use Vercel environment variables for API keys.
